extends layouts/main

block content
    .form-container
        form(action=routes.editVideo, method="post")
            input(type="text", placeholder="제목", name="title")
            input(type="text", placeholder="설명", name="description")
            input(type="submit", value="콘텐츠 업데이트")
        a.form-container__link(href=routes.deleteVideo) 콘텐츠 삭제