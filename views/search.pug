extends layouts/main
include mixins/videoBlock

block content
    .search__header
        h3 #{searchingBy} 로 검색합니다.
    .search__videos
        each item in videos 
            +videoBlock({
                title: item.title,
                views: item.views,
                videoFile: item.videoFile
            })